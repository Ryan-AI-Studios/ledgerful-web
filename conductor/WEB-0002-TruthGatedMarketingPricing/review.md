# WEB-0002 Review

## Status

Draft planning packet ready for implementation. No page changes have started.

## Sources Consulted

- `C:\dev\Roadmap.md`: WEB-1, Commercial Packaging, Pricing Notes.
- `AGENTS.md`, `PRODUCT.md`, and `docs/ToDo.md`.
- `src/lib/content/pricing.ts`, `features.ts`, and
  `src/app/pricing/page.tsx`.
- `C:\dev\ChangeGuard\conductor\conductor.md`.
- `C:\dev\ledgerful-frontend\conductor\conductor.md`.
- `C:\dev\ChangeGuard\docs\Frontend-Notes.md`.
- `C:\dev\ledgerful-frontend\docs\Backend-Notes.md`.
- Impeccable brand register and the existing Ledgerful visual tokens.

AI-Brains preflight was attempted during planning and timed out; no historical
claim decision from AI-Brains is treated as verified for this packet.

## Planning Findings

### High: Hosted edition name reads as live beta

Current copy says `Team Hosted Beta` while the edition state is
`hosted planned`. Rename it during implementation.

### High: Enterprise name implies an unverified contact path

Current copy says `Enterprise / Contact`, but no monitored contact path is a
resolved launch fact. Remove `Contact` unless evidence is added.

### Medium: Equal card treatment blurs availability

All four editions use equivalent pricing cards, making available local and
future hosted capabilities look commercially parallel. Replace this with an
edition ladder and evidence matrix.

### Medium: Feature rows lack structured evidence

Current rows carry state labels but no machine-auditable evidence/caveat field.
Strengthen the content model before revising copy.

### Medium: Several beta rows require revalidation

Re-check `Portfolio reports`, team signatures/devices, MCP stdio tools, and the
GitHub Action setup path. Implementation must downgrade, qualify, or remove any
row that lacks current release evidence.

## Locked Truth Boundaries

- Local daemon/dashboard remain local-only and ephemeral-token protected.
- Local SOC2 evidence is a ZIP export, not a hosted portal.
- Team sync is the local-first signed directory transport, not Supabase-hosted
  sync.
- MCP and GitHub Action public install paths remain release-smoke blocked.
- Hosted portfolio, GitHub App, billing, team roles, and retained audit state
  remain hosted planned.
- SSO/SAML/OIDC, SCIM, RBAC, hosted audit export, retention, support SLA, and
  optional self-hosting remain enterprise planned.
- No source upload is required for local paid value.
- No final paid amounts are authorized by this track.

## Design Direction

Reference: a procurement calibration sheet paired with engineering evidence,
using the existing steel-blue/graphite/brass instrument identity.

Preserve:

- Archivo + JetBrains Mono.
- Existing OKLCH tokens.
- Compact state pills and evidence-first voice.

Avoid:

- Generic four-column SaaS pricing cards.
- Equal visual weight for available and planned editions.
- Gradient text, glassmorphism, fake metrics, fake logos, and decorative
  terminal cosplay.

## Implementation Review

Not started. Critical/high planning findings must be resolved before completion.
